syntax = "proto3";

package orgs.v1;

import "tagger/tagger.proto";
import "validate/validate.proto";

// GetStatusResponse returns a broken down status of each component in the org
message GetStatusResponse {
  // provisioned (started, error, finished)
  // server active
  // org runner connected
}

message GetStatusRequest {
  string org_id = 2 [
    (validate.rules).string.len = 26,
    (tagger.tags) = "faker:\"len=26\""
  ];
}

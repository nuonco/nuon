# Attributions Python Library

Library for managing attribution code across our codebase. Supports both `website-urls` and `redirect-urls` and comprises the tracking and attribution of them.

All data is written to datastore under the `attributions` namespace.

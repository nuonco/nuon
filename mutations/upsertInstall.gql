mutation UpsertInstall($name: String!, $appId: ID!) {
  upsertInstall(input: {name: $name, appId: $appId}) {
    id
    name
    workflowID
  }
}

## variables

name: ${INSTALL_NAME}
appId: ${APP_ID}

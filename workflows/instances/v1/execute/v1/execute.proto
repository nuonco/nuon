syntax = "proto3";

package instances.v1.execute.v1;

import "plan/v1/plan.proto";

// ExecuteRequest represents a workflow input for creating a deployment
message ExecuteRequest {
  .plan.v1.PlanRef plan = 1;
}

// ExecuteResponse returns the response from an instance provision workflow
message ExecuteResponse {}

# Echo Module

This is a sample module for working with Terraform Cloud. This module is mainly for testing out Nuon's support of private terraform modules.

## Inputs

* `input` - a string value to be set

## Outputs

* `output` - the value that was set as the `input` value (for echoing)
* `account_id` - the aws account id


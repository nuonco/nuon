---
title: 'Google Cloud'
description: "Deploy and manage BYOC apps on Google Cloud."
icon: 'google'
boost: 1.5
keywords: ["GCP BYOC", "Google Cloud BYOC", "service account permissions GCP", "Workload Identity", "GCP project setup", "GKE", "install stack GCP", "Terraform install stack", "Infrastructure Manager", "Spacelift", "no console link", "gcloud auth application-default login"]
---

## Configuration

To deploy an app to GCP, configure it to use a GCP sandbox and runner. We provide a [GKE sandbox](https://github.com/nuonco/gcp-gke-sandbox) you can use to get started, which provisions a GKE cluster and the IAM bindings the runner needs. Create inputs for the public and private domains, since those will be different for each install.

```toml sandbox.toml
# sandbox

terraform_version = "1.11.3"

[public_repo]
directory = "."
repo      = "nuonco/gcp-gke-sandbox"
branch    = "main"

[vars]
cluster_name         = "n-{{.nuon.install.id}}"
public_root_domain   = "{{ .nuon.inputs.inputs.domain }}"
internal_root_domain = "internal.{{ .nuon.inputs.inputs.domain }}"

[[var_file]]
contents = "./sandbox.tfvars"
```

```toml sandbox.tfvars
additional_namespaces = ["whoami"]
```

```toml runner.toml
# runner

runner_type = "gcp"
helm_driver = "configmap"
```

## Installation

There is no one-click console link on Google Cloud. Nuon generates the install stack as Terraform only, and your customer applies it with their own credentials using whichever Terraform workflow they already have. The **await install stack** step of the provision workflow offers up to three tabs:

- **TF Module** (recommended, feature flag): a snippet that adds the published [`nuonco/stack/gcp`](https://registry.terraform.io/modules/nuonco/stack/gcp/latest) module to your customer's own Terraform project. See [Provision Stacks with a Terraform Module](/guides/provision-stacks-with-terraform-module).
- **Terraform**: generated `inputs.auto.tfvars` and `secrets.auto.tfvars` files for the open-source [`install-stacks/gcp`](https://github.com/nuonco/install-stacks) module, applied with the Terraform command-line interface (CLI). The steps are below.
- **Spacelift** (feature flag): a Spacelift blueprint and a `spacelift.tf` so the customer can run the stack inside their Spacelift account. See [Provision Stacks with Spacelift](/guides/provision-stacks-with-spacelift).

Any other Terraform runner works too, including Google Cloud's Infrastructure Manager, which runs Terraform natively. Nuon does not generate an Infrastructure Manager configuration; the customer points it at the module and tfvars like any other Terraform pipeline.

For a side-by-side view of what customers do on each cloud, see [Stacks](/concepts/stacks#what-your-customer-does-on-each-cloud).

### Create an Install

Navigate to the Installs tab in the Nuon dashboard, and click on the "Create Install" button, and select the app you want to install.

This will kick off a provision workflow. The stack will be generated and pre-configured `inputs.auto.tfvars` and `secrets.auto.tfvars` files will be provided for it. The tfvars files are templated with the install ID, runner ID, API token, and IAM (Identity and Access Management) permissions, and can be shared with your customer.

### Install the Stack

Your customer must be authenticated to the GCP project they want to install your app in (typically via `gcloud auth application-default login`) and have permissions to create the IAM bindings, service accounts, and resources defined in the stack module.

The **Terraform** tab provides four steps the customer can follow:

1. **Clone the install stack module**

   ```bash
   git clone https://github.com/nuonco/install-stacks.git
   cd install-stacks/gcp
   ```

2. **Configure remote state (recommended)** — create a `backend.tf` pointing at a Google Cloud Storage (GCS) bucket so the install state is durable.

   ```hcl
   terraform {
     backend "gcs" {
       bucket = "<your-state-bucket>"
       prefix = "nuon/<install-id>"
     }
   }
   ```

3. **Save the install configuration** — copy or download the `inputs.auto.tfvars` and `secrets.auto.tfvars` content shown in the dashboard and save both files next to the module.

4. **Apply with Terraform**

   ```bash
   terraform init && terraform apply
   ```

   Both `.auto.tfvars` files are loaded automatically, so no `-var-file` flag is needed.

The customer is prompted for `gcp_project_id` and `gcp_region` at apply time unless those are pre-populated in the install config (`gcp_account.project_id` / `gcp_account.region`), in which case they're injected into the tfvars and applied automatically.

Once the runner phones home, the await install stack step completes and the install proceeds.

## Try it with a sample app

We maintain [`gke-simple`](https://github.com/nuonco/example-app-configs/tree/main/gke-simple), a sample app that uses the GKE sandbox to deploy a `whoami` workload with a certificate and load balancer.

To try it:

- Install the [CLI](../cli)
- Clone the [example apps repo](https://github.com/nuonco/example-app-configs)
- `cd` into the `gke-simple` directory
- Run `nuon auth login`
- Run `nuon apps create -n gke-simple`
- Run `nuon apps sync`
- Open the Nuon dashboard and create an install

## Updating

If you make changes to the install stack template, the install must be reprovisioned to update the stack.

1. From the install's Overview page, click on "Reprovision install" in the "Manage" drop-down menu. This will trigger a reprovision workflow.
1. A new Terraform stack version and updated `inputs.auto.tfvars` and `secrets.auto.tfvars` files will be generated.
1. Send the updated files to your customer to re-apply with `terraform apply`.

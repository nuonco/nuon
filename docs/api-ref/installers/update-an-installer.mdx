---
title: 'Update an installer'
description: 'PATCH /v1/installers/{installer_id} — update an installer''s metadata, slug, branding, or the set of inputs that customers see when launching an install.'
openapi: patch /v1/installers/{installer_id}
---
---
title: 'Get an installer'
description: 'GET /v1/installers/{installer_id} — fetch a single installer by ID, including its app, slug, branding, and the inputs customers fill in to install.'
openapi: get /v1/installers/{installer_id}
---
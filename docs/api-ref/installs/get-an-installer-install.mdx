---
title: 'Get an installer install'
description: 'GET /v1/installer/{installer_slug}/install/{install_id} — fetch an install created from a public installer slug, used by the self-service install flow.'
openapi: get /v1/installer/{installer_slug}/install/{install_id}
---
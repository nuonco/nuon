---
title: 'Get an install component'
description: 'GET /v1/installs/{install_id}/component/{component_id} — fetch a single component within an install, including its current status, deploys, and outputs.'
openapi: get /v1/installs/{install_id}/component/{component_id}
---
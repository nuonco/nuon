---
title: 'Create an install from an installer'
description: 'POST /v1/installer/{installer_slug}/installs — create a new app install from a public installer slug, as part of the self-service install flow for customers.'
openapi: post /v1/installer/{installer_slug}/installs
---
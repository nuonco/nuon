---
title: "Get install deploy plan"
description: "Retrieve the runner plan for a specific install deploy to inspect what will change in the customer cloud account (deprecated endpoint)."
openapi: get /v1/installs/{install_id}/deploys/{deploy_id}/plan
---
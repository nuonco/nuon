---
title: 'Get install deploy plan'
description: 'GET /v1/installs/{install_id}/deploys/{deploy_id}/plan — fetch the Terraform, Helm, or manifest plan output for a single install deploy before apply.'
openapi: get /v1/installs/{install_id}/deploys/{deploy_id}/plan
---
---
title: 'Get release steps'
description: 'GET /v1/releases/{release_id}/steps — fetch every release step for a release, with each step''s target installs, status, delay, and per-install deploys.'
openapi: get /v1/releases/{release_id}/steps
---
---
title: 'Create a release'
description: 'POST /v1/components/{component_id}/releases — create a new release for a component, picking the targeted builds and the install groups to deploy to.'
openapi: post /v1/components/{component_id}/releases
---
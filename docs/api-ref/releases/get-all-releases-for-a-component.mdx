---
title: 'Get all releases for a component'
description: 'GET /v1/components/{component_id}/releases — list every release for a component, with status, release steps, and the app config version each came from.'
openapi: get /v1/components/{component_id}/releases
---
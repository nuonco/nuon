---
title: "Get component build plan"
description: "Retrieve the runner plan for a specific component build — useful for inspecting how a component will be built (deprecated endpoint)."
openapi: get /v1/components/{component_id}/builds/{build_id}/plan
---
---
title: "Get all builds for a component"
description: "List every component build for a Nuon component — useful for inspecting build history and selecting a specific build to deploy."
openapi: get /v1/components/{component_id}/builds
---
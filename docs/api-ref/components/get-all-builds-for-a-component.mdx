---
title: 'Get all builds for a component'
description: 'GET /v1/components/{component_id}/builds — list every build for a component, including build status, OCI artifact, and the app config version that produced it.'
openapi: get /v1/components/{component_id}/builds
---
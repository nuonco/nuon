---
title: 'Update an app installer'
description: 'PATCH /v1/installers/{installer_id} — update an installer''s metadata, slug, branding, or the set of inputs customers fill in when self-serving an install.'
openapi: patch /v1/installers/{installer_id}
---
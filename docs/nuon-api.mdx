---
title: 'API'
description: "Get started using the Nuon API endpoints."
boost: 10
keywords: ["API", "api", "Nuon API", "REST API", "control plane API", "API reference", "API token", "access token", "Swagger", "OpenAPI", "programmatic install creation", "Bearer token"]
---

You'll need an access token to use the Nuon API. Create an org-scoped [API token](/concepts/api-tokens) from the Nuon [Dashboard][1] or the Nuon CLI. Tokens are shown only once, so store them somewhere safe.

```sh
nuon login
nuon orgs api-tokens create --name my-token
```

For automation and CI/CD, use a [service account](/concepts/service-accounts) — a named machine identity you can assign a role and mint multiple tokens for.

## Using the Swagger API Reference

You can call the Nuon API endpoints from the API Reference by visiting the [Swagger API Reference](https://api.nuon.co/docs/index.html)

## Using the API 

You can call the Nuon API endpoints from the API Reference by adding `Bearer <your-api-token>` to the "Authorization" field in each endpoint card. Verify your API token with the [current-user](/api-ref/general/get-current-user) endpoint.

[1]: https://app.nuon.co

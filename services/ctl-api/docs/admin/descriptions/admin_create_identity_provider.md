Creates a global identity provider (no org_id). Several providers can share a provider type; use `name` to label them
on the sign-in page. Registering the same application twice (same type, client id and, for OIDC, issuer) is rejected.

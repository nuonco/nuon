Add an AWS delegation config.

**NOTE** this is experimental, until we expose this in the terraform provider, config file and write it with the sandbox
config.

If you need to modify a sandbox config _after_ setting a delegation config, the config must be reset using this
endpoint.

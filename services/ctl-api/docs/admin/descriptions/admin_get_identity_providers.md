List all identity providers, returning a slim representation (id, provider type, name, client id, enabled status, and
whether the provider comes from environment variables or the database) used to determine whether a specific identity
provider exists or is enabled.

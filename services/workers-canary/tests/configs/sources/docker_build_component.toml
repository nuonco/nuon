#:schema http://localhost:8081/v1/general/config-schema?source=docker_build
name = "toml_docker_build"
type = "docker_build"

dockerfile = "Dockerfile"

[connected_repo]
directory = "deployment"
repo = "powertoolsdev/mono"
branch = "main"

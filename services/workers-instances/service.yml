---
type: worker
env:
  DISABLE_METRICS: true
env_secrets:
  DEPLOYMENT_BOTS_SLACK_WEBHOOK_URL:
    name: deployment-bots-slack-webhook-url
    key: slack_webhook_url
local_cmds:
  - go run . all
earthly_cmd: all
inject_support_role_credentials: true

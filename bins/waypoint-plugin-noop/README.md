# Waypoint Plugin Noop

Often, we need an empty build, so we can properly trigger an install. This plugin introduces both a noop build and a noop registry that can be used in place of most types of build/artifacts, when we actually want to deploy independently.

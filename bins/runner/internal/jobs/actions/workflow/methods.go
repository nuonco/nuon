package workflow

import (
	"context"
)

func (h *handler) Outputs(ctx context.Context) (map[string]interface{}, error) {
	// read outputs from the workspace file
	return map[string]interface{}{}, nil
}

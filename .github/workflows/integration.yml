---
name: integration
on:
  push:
    branches:
      - main

permissions:
  contents: write
  id-token: write
  issues: read
  packages: write
  pull-requests: write
  statuses: write
  actions: read
  checks: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  # if the PR was labeled `integration`, then we automatically will run a integration on it, when the PR lands.
  merged_pr:
    name: Get merged PR
    runs-on: ubuntu-latest
    outputs:
      integration: ${{steps.output.outputs.integration}}
    steps:
      - uses: actions-ecosystem/action-get-merged-pull-request@v1
        name: get merged pr
        id: merged_pr
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: update integration output
        id: output
        if: contains( steps.merged_pr.outputs.labels, 'integration')
        run: echo "integration=true" >> "$GITHUB_OUTPUT"

  terraform:
    name: Get terraform outputs
    uses: ./.github/workflows/terraform_outputs.yml
    needs:
      - merged_pr
    secrets: inherit
    if: ${{ needs.merged_pr.outputs.integration == 'true' }}
    with:
      dir: infra/eks
      cache-key: infra-eks-stage-nuon
      workspace: stage-nuon

  run-integration-tests:
    name: 🌏 test
    runs-on: [ubuntu-latest-16-cores, linux]
    if: ${{ needs.merged_pr.outputs.integration == 'true' }}
    needs:
      - merged_pr
      - terraform
    permissions:
      contents: read
      packages: write
    timeout-minutes: 10
    steps:
      - name: Fetch terraform outputs
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.terraform.outputs.env_cache_key }}

      - name: Copy terraform outputs to environment
        run: |
          cat "${{ needs.terraform.outputs.tf_env }}" >> "$GITHUB_ENV"

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup twingate
        uses: twingate/github-action@v1
        with:
          service-key: ${{ env.TFO_TWINGATE_SERVICE_ACCOUNTS_GITHUB_ACTIONS_TOKEN }}

      - name: Setup for CI
        uses: powertoolsdev/mono/actions/setup-ci@main
        with:
          tfcloud_token: ${{ secrets.TF_API_KEY }}
          setup_helm: "false"
          github_token: ${{ secrets.GITHUB_TOKEN }}

      # NOTE(jm): the dns chain using twingate does not seem to work with the tests running in the image. This is most
      # likely because netgo is being used (instead of cgo), to resolve the domain.
      #
      # We attempt to resolve it here, and pass in an actual IP address, to hopefully work around this.
      - name: Resolve internal api
        run: |
          echo "INTERNAL_API_HOST=$(dig +short ctl.nuon.us-west-2.stage.nuon.cloud)" >> $GITHUB_ENV

      - name: Earthly test target
        working-directory: services/ctl-api
        run: |
          earthly \
            --allow-privileged \
            ${{ env.EARTHLY_EXTRA_ARGS }} \
            --ci \
            --remote-cache=ghcr.io/${{ github.repository }}-pkg:cache \
            +integration \
            --api_url=https://ctl.stage.nuon.co \
            --internal_api_url=http://${{ env.INTERNAL_API_HOST }}
  status_slack_notifications:
    name: update integration channel
    if: needs.merged_pr.outputs.integration == 'true' && always()
    needs:
      - merged_pr
      - terraform
      - run-integration-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: martialonline/workflow-status@v3
        id: check
      - name: Slack Notification
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_ICON_EMOJI: 🤖
          SLACK_USERNAME: github-bot
          SLACK_WEBHOOK: ${{ secrets.INTEGRATION_SLACK_WEBHOOK_URL }}
          SLACK_COLOR: ${{steps.check.outputs.status}}
          SLACK_MESSAGE: |
            ${{ steps.check.outputs.status == 'success' && '🏁🏁' || '🚨🚨' }} integration test finished
            *stage* - true
            *status* - ${{steps.check.outputs.status}}
      - name: Update datadog
        uses: masci/datadog@v1
        with:
          api-key: ${{ secrets.DATADOG_API_KEY }}
          api-url: "https://us5.datadoghq.com"
          metrics: |
            - type: "count"
              name: "integration.runs.count"
              value: 1.0
              host: ${{ github.repository_owner }}
              tags:
                - "branch:${{ github.head_ref }}"
                - "status:${{ steps.check.outputs.status }}"

---
name: test
"on":
  workflow_call:
    inputs:
      service:
        type: string
        required: true

defaults:
  run:
    shell: bash

jobs:
  test:
    name: 🌏 test
    runs-on: mono
    permissions:
      contents: read
      packages: write
    timeout-minutes: 9
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup nuonctl 
        uses: powertoolsdev/mono/actions/setup-nuonctl@jm/nuonctl-commands

      #- name: Lint
        #working-directory: services/${{inputs.service}}
        #run: |
          #CI=true nuonctl tests run --name=${{inputs.service}} --test='lint'

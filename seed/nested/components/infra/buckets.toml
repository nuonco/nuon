#:schema http://localhost:8081/v1/general/config-schema?type=terraform
name = "buckets"
type = "terraform_module"
terraform_version = "1.8.4"

[connected_repo]
directory = "bins/nuonctl/components/s3_terraform"
repo = "powertoolsdev/mono"
branch = "main"

[vars]
region = "{{.nuon.install_stack.outputs.region}}"
install_id = "{{.nuon.install.id}}"
